node_modules
dist
.output
.turbo
coverage
.env
.env.local
*.log
.DS_Store
data/
*.db
*.db-journal
pnpm-lock.yaml.backup
.vite
.nuxt
.next
.dart_tool
build
target

# Test artifacts
test-results/
playwright-report/
blob-report/
.e2e-state.json

# Generated wallet keys — never commit!
.wallet.local.json
.wallet.*.json

# Host-specific docker-compose overrides
docker-compose.override.yml

# Claude Code local tooling state (session IDs, locks, settings)
.claude/
docs/design/

# Stitch design assets (local mockups, not committed)
Stitch/

# VitePress build artifacts
apps/playground/.vitepress/cache/
apps/playground/.vitepress/dist/
